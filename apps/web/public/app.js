const root = document.getElementById("root");
if (root) {
  root.innerHTML =
    "<main style='padding:20px;font-family:Arial,sans-serif'>" +
    "<h1>Painel Operacional</h1>" +
    "<p>Build do frontend React nao encontrado. Execute npm run build para gerar /dashboard/app.js e /dashboard/styles.css.</p>" +
    "</main>";
}
